A suggestion has been added to your watched proposal ‘<%= @proposal.title %>’. Visit <%= proposal_path(@proposal) %> to it.

This mail was sent by UIKonf.

If you don't want to receive these notifications any more, visit <%= proposal_path(@proposal) %> and you can switch them off.
