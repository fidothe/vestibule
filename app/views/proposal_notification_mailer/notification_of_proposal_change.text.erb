Your watched proposal ‘<%= @proposal.title %>’ has been updated. Visit <%= proposal_url(@proposal) %> to see the changes.

This mail was sent by UIKonf.

If you don't want to receive these notifications any more, visit <%= proposal_url(@proposal) %> and you can switch them off.
